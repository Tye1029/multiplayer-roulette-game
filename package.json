{
  "name": "torn-xans-scratch-ticket",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "clean:lamp": "node scripts/clean-legacy-lamp-assets.mjs",
    "validate:lamp": "node scripts/validate-lamp.mjs",
    "build": "npm run clean:lamp && node scripts/inject-lamp-assets.mjs && npm run validate:lamp && echo 'Static Netlify site - validation complete'"
  },
  "dependencies": {
    "@netlify/blobs": "^8.1.0",
    "@netlify/database": "latest"
  }
}
