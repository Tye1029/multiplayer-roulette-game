name: Build TerrorCounter No-INI BA2

on:
  push:
    branches:
      - chatgpt/terrorcounter-noini
  pull_request:
    branches:
      - main

permissions:
  contents: read

jobs:
  placeholder:
    if: github.ref == 'refs/heads/__never__'
    runs-on: ubuntu-latest
    steps:
      - run: echo "Temporary activation stub for ChatGPT TerrorCounter no-INI build"
