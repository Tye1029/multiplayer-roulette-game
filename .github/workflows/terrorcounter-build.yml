name: Build TerrorCounter BA2

on:
  push:
    branches:
      - chatgpt/terrorcounter-build

permissions:
  contents: read

jobs:
  placeholder:
    if: github.ref == 'refs/heads/__never__'
    runs-on: ubuntu-latest
    steps:
      - run: echo "Temporary activation stub for ChatGPT TerrorCounter build"
